---
import QuestionLayout from '@layouts/QuestionLayout.astro';

const relatedLinks = [];
const _body = "<p>A Traverse contract starts as a TOML file and ends when you remove the last version from the registry. Between those two points it goes through five stages: authoring, registration, active use, versioned updates, and retirement.</p>\n\n      <h2>1. Authoring</h2>\n      <p>You write the contract TOML alongside your Rust implementation. The contract defines what the capability accepts, what it returns, and what conditions hold. You iterate on both the contract and the code together. The contract is the spec — write it first if possible.</p>\n\n      <h2>2. Registration</h2>\n      <p>Once the implementation compiles to WASM, you register the contract and the binary together with the CLI. The registry validates the contract schema and rejects invalid contracts before storing anything. A successful registration makes the capability available to callers.</p>\n\n      <h2>3. Active use</h2>\n      <p>Callers — applications, AI agents via MCP, other services — discover and call the capability. The runtime enforces the contract on every call. Trace artifacts accumulate as evidence of every execution.</p>\n\n      <h2>4. Versioned updates</h2>\n      <p>Requirements change. You register a new version: update the TOML version string, update the Rust implementation, compile, register. The old version stays available. Callers migrate on their own schedule. See <a href=\"/questions/what-is-contract-versioning.html\">contract versioning</a> for how to manage breaking vs. non-breaking changes.</p>\n\n      <h2>5. Retirement</h2>\n      <p>When no active callers pin to an old version, you remove it from the registry. Any caller that still references it will fail at discovery time with a clear registry error — not a silent behavior change. That is intentional: retirement forces callers to migrate explicitly.</p>\n\n      <p>The governing spec — the document behind the contract — lives outside the registry and evolves independently. See <a href=\"/questions/what-is-a-governing-spec.html\">what is a governing spec</a> for how those two relate.</p>";
const _jsonLd = "{\"@context\":\"https://schema.org\",\"@type\":\"FAQPage\",\"mainEntity\":[{\"@type\":\"Question\",\"name\":\"What is the contract lifecycle in Traverse?\",\"acceptedAnswer\":{\"@type\":\"Answer\",\"text\":\"A Traverse contract lifecycle runs from authoring through registration, active use, versioned updates, and eventual retirement. Each stage has specific actions: you write the contract TOML, register it with a WASM binary, callers use it, you register new versions as requirements change, and you remove old versions when no callers depend on them.\"}}]}";
---
<QuestionLayout
  title={"What is the contract lifecycle in Traverse?"}
  description={"A Traverse contract goes through authoring, registration, active use, versioning, and eventual retirement. Learn what happens at each stage."}
  canonical={"https://traverse-framework.com/questions/what-is-the-contract-lifecycle.html"}
  category={"Contracts"}
  relatedLinks={relatedLinks}
  jsonLd={_jsonLd}
>
  <Fragment set:html={_body} />
</QuestionLayout>
