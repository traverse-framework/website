---
import QuestionLayout from '@layouts/QuestionLayout.astro';

const relatedLinks = [];
const _body = "<p>Function calling — as implemented by OpenAI, Anthropic, and others — gives an AI agent a JSON schema describing a function and lets it call that function. Traverse adds three things that raw function calling does not provide: runtime contract enforcement, WASM sandbox isolation, and a trace artifact on every execution.</p>\n\n      <h2>Schema description vs. runtime enforcement</h2>\n      <p>With standard function calling, the JSON schema in the tool definition is advisory. It helps the agent construct a valid call, but nothing stops the agent from sending invalid inputs if the LLM decides to. The receiving function sees whatever the agent sent. With Traverse, the contract is enforced at runtime. Invalid inputs are rejected before execution — the WASM binary never runs on bad data.</p>\n\n      <h2>Arbitrary functions vs. sandboxed capabilities</h2>\n      <p>Function calling connects an agent to any callable code: a Python function, a Node.js handler, a Lambda. That code can do anything — read files, make network calls, modify state. Traverse capabilities run in a WASM sandbox. They have no access to host resources unless explicitly granted. For sensitive business logic, this isolation matters.</p>\n\n      <h2>No audit trail vs. trace artifacts</h2>\n      <p>Function calling gives you whatever logging you build yourself. Traverse produces a trace artifact on every call as a side effect of the execution model. The artifact records the inputs, the precondition results, the WASM execution, the postcondition results, and the final output. You get an audit trail without building one.</p>\n\n      <h2>When to use each</h2>\n      <ul>\n        <li>Function calling: fast integration, arbitrary code, no portability requirement</li>\n        <li>Traverse: business logic that needs enforcement, portability across runtimes, and auditability</li>\n      </ul>\n\n      <p>Traverse is not a replacement for function calling in general. It is the right choice when the function being called is business-critical and you need the contract enforced rather than suggested.</p>";
const _jsonLd = "{\"@context\":\"https://schema.org\",\"@type\":\"FAQPage\",\"mainEntity\":[{\"@type\":\"Question\",\"name\":\"How is Traverse different from function calling?\",\"acceptedAnswer\":{\"@type\":\"Answer\",\"text\":\"Function calling exposes arbitrary functions to AI agents with a schema description but no runtime enforcement. Traverse adds three things function calling lacks: precondition and postcondition enforcement at call time, WASM sandbox isolation so capabilities cannot access host resources arbitrarily, and a trace artifact on every execution for auditability.\"}}]}";
---
<QuestionLayout
  title={"How is Traverse different from function calling?"}
  description={"Function calling gives AI agents raw function access. Traverse adds contract enforcement, WASM isolation, and a trace artifact on every call. Learn the key differences."}
  canonical={"https://traverse-framework.com/questions/how-is-traverse-different-from-function-calling.html"}
  category={"Comparisons"}
  relatedLinks={relatedLinks}
  jsonLd={_jsonLd}
>
  <Fragment set:html={_body} />
</QuestionLayout>
